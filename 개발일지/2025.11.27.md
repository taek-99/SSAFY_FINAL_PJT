# 개발 일지 2차

## 전날 완료 사항
- Not-found 화면 & 로그인 미인증 시 main 화면 접근 시 뜨는 화면 구성
- 비밀번호 변경 (이메일 인증 후 접근 가능) 페이지 구성
- Home 화면 구성
- 응급요원 자격 인증 페이지 구현


## Today 진행 예정 (3hr)
- 네이버 지도 API 연결 후 현재 위치입력
- 응급요원은 자격 있을때만 화면 접근 가능
- 로그아웃 기능
- 로그인 시 갈수 있는 곳 & 로그인 안했을때만 갈 수 있는 곳


## 학습 내용
- 네이버 지도 client key 연결
    - 프로젝트 파일에 index.html파일 생성 후
    ```html
    <script type="text/javascript" src="https://oapi.map.naver.com/openapi/v3/maps.js?ncpKeyId=CLIENT KEY&callback=CALLBACK_FUNCTION"></script>
    ```
    head에 입력
    - 페이지 파일에서 onMounted 함수로 호출

- 로그아웃 기능
    - 토큰 & 유저 정보 삭제 후 로그인 화면 이동

- 응급요원 자격 증명
    - 기존 유저 정보 통신시 role이라는 변수 추가하여 통신 진행
    - default값으로 false(일반용) 상태고 role 인증 받으면 true(의료진)으로 변경


## 완료된것
- 네이버 지도 제외하고 완료


## 실패한것 + 이유
- 지도 불러오고 현재위치 불러오는것까진 성공
    - but 로컬에서만 가능하고 ec2서버에선 불가능
        - 이유: public ip에서 접근하면 크롬 자체에서 차단
        - 해결방법: Https 인증을 받아야 접근가능
                   차후 도메인 등록후 시도 예정